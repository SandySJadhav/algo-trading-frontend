import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Profile | Algo - To the future",
  description: "Algo - To the future",
};

const Profile = () => {
  return <div>Profile</div>;
};

export default Profile;
